<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChatLock : Favourate</title>
</head>
<body>
    
<div class="container mx-auto px-4 py-6">
  <h2 class="text-2xl font-semibold mb-4">Favourite Users</h2>

  <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% if (favourites.length === 0) { %>
      <p>No favourite users added yet.</p>
    <% } else { %>
      <% favourites.forEach(user => { %>
        <li class="bg-white shadow-md rounded-lg p-4">
          <h3 class="text-lg font-bold"><%= user.name %></h3>
          <p class="text-sm text-gray-600">Email: <%= user.email %></p>
          <% if (user.unreadCount > 0) { %>
            <p class="text-red-600 font-semibold">Unread messages: <%= user.unreadCount %></p>
          <% } %>
        </li>
      <% }) %>
    <% } %>
  </ul>
</div>


</body>
</html>